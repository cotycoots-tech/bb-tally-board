services:
  - type: web
    name: bb-tally-board
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
    disk:
      name: tally-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
